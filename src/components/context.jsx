import React from "react";

// For Home Component
export const contextHome = React.createContext();
export const HomeProvider = contextHome.Provider;
// For Blog Component
export const contextBlog = React.createContext();
export const BlogProvider = contextBlog.Provider;
